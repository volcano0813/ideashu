# Optional. The Phase 1 app, SQLite store, deterministic cover renderer, and MCP work without provider keys.
IDEASHU_PORT=3210

# Configure image-provider credentials only in your desktop-agent host or local environment.
# Never prefix secrets with VITE_ and never commit a populated .env file.
SILICONFLOW_API_KEY=
